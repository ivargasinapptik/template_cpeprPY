# template_cpeprPY
Esto es un template para comenzar una aplicacion para Desktop con el lenguaje de python. 
# Variables utilizadas hasta el momento 
pip install python-dotenv
pip install bcrypt
pip install psycopg2-binary
pip install customtkinter
